/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'images.unsplash.com',
        'source.unsplash.com',
        'randomuser.me',
        // Add any other domains you plan to use
      ],
    },
  }

export default nextConfig;
